import 'package:flutter/material.dart';

class CalendarPreviewView extends StatelessWidget {
  const CalendarPreviewView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
